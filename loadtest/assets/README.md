Umiesc tutaj prawidlowy plik testowy `sample.heic` (lub podaj inna sciezke przez `HEIC_FILE`).

Ten plik jest wykorzystywany przez test obciazeniowy Locust.

Przyklad:

```bash
cp /sciezka/do/twojego/pliku.heic ./loadtest/assets/sample.heic
```
